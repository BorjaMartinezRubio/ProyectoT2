
import java.util.*;
public class gestion {
	ArrayList<vehiculos> listaCoches=new ArrayList<vehiculos>();
	protected void insertarVehiculos(vehiculos vehiculo) {
		
		boolean control=true;
		ArrayList<vehiculos> listaCoches=new ArrayList<vehiculos>();
		Iterator<vehiculos>vehiculoIterator=listaCoches.iterator();
		while(vehiculoIterator.hasNext()&& control) {
			vehiculos vehiculo1=vehiculoIterator.next();
			if(vehiculo1.getBastidor().equalsIgnoreCase(vehiculos.getBastidor())&& vehiculo1.getBastidor().equalsIgnoreCase(vehiculos.getMatricula())vehiculo1.getBastidor().equalsIgnoreCase(vehiculos.getMatricula()) {
			
			
		}
	}

}
